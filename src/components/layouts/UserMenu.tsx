import * as React from 'react';

import { Icon } from 'semantic-ui-react';

const UserMenu = () => (
    <div>
        <span>UserName</span><Icon name="user circle outline" size={'large'}/>
    </div>
);

export default UserMenu;